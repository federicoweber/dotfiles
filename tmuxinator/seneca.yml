# ~/.tmuxinator/seneca.yml

name: seneca
root: ~/Projects/seneca-env

windows:
  - : fixcolors && nvim -S Session.vim
  - : docker-compose up -d && fixcolors && docker ps
  - :
      layout: tiled
      panes:
        - docker-compose logs -f finance
        - docker-compose logs -f web
        - docker-compose logs -f users
        - docker-compose logs -f mongo
